To run these test you will need to have a local instance of the Chirp.Web project running.

open another terminal, navigate to ```.\src\Chirp.Web``` and run:
    ```dotnet run```
    
then open another terminal, navigate to ```.\test\Chirp.Web.Test``` and run:
    ```dotnet test```